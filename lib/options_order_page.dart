import 'package:flutter/material.dart';

class OptionsQueryPage extends StatelessWidget {
  const OptionsQueryPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        '選擇權查詢頁面',
        style: TextStyle(fontSize: 24, color: Colors.teal[700]),
      ),
    );
  }
}
